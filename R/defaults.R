require(Defaults)
#this slows down execution considerably, therefore commented out
# setDefaults("momentum",na.pad=F); useDefaults("momentum"); unDefaults("momentum")
# setDefaults("ROC",na.pad=F); useDefaults("ROC"); unDefaults("ROC")
# setDefaults("lag.xts",na.pad=F); useDefaults("lag.xts"); unDefaults("lag.xts")
# unsetDefaults("momentum", confirm=F)
